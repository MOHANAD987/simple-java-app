name: test1 workflow

on:
  push:
    branches:
      - main

jobs:
  test1-workflow-job:
    name: test job
    runs-on: ubuntu-latest

    steps:
      - name: checkout steps
        uses: actions/checkout@v3

      - name: run maven
        run: mvn clean package
